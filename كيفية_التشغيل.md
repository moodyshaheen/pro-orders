# دليل تشغيل مشروع Pro-Orders

## 📋 نظرة عامة على المشروع

المشروع يتكون من 3 أجزاء رئيسية:
1. **Backend** - السيرفر (Node.js/Express) - يعمل على المنفذ 4000
2. **Frontend** - واجهة المستخدم (React) - يعمل على المنفذ 5173 (افتراضي)
3. **Admin** - لوحة التحكم (React) - يعمل على منفذ مختلف

---

## 🚀 خطوات التشغيل

### 1️⃣ تشغيل Backend (السيرفر)

افتح Terminal/PowerShell في مجلد `backend` وقم بتنفيذ:

```bash
cd backend
npm run server
```

**النتيجة المتوقعة:**
- السيرفر سيعمل على: `http://localhost:4000`
- ستظهر رسالة: "Server Started on http://localhost:4000"
- ستظهر رسالة: "Db Conected" عند الاتصال بقاعدة البيانات

---

### 2️⃣ تشغيل Frontend (واجهة المستخدم)

افتح Terminal/PowerShell جديد في مجلد `frontend` وقم بتنفيذ:

```bash
cd frontend
npm run dev
```

**النتيجة المتوقعة:**
- التطبيق سيعمل على: `http://localhost:5173` (أو منفذ آخر إذا كان 5173 مشغول)
- افتح المتصفح على العنوان الذي سيظهر في Terminal

---

### 3️⃣ تشغيل Admin (لوحة التحكم)

افتح Terminal/PowerShell جديد في مجلد `admin` وقم بتنفيذ:

```bash
cd admin
npm run dev
```

**النتيجة المتوقعة:**
- لوحة التحكم ستعمل على: `http://localhost:5174` (أو منفذ آخر)
- افتح المتصفح على العنوان الذي سيظهر في Terminal

---

## ⚠️ ملاحظات مهمة

1. **يجب تشغيل Backend أولاً** قبل تشغيل Frontend أو Admin
2. **يحتاج كل جزء Terminal منفصل** - لا يمكن تشغيلهم في نفس Terminal
3. **تأكد من تثبيت المكتبات** - إذا واجهت مشاكل، قم بتشغيل:
   ```bash
   npm install
   ```
   في كل مجلد (backend, frontend, admin)

---

## 🔧 حل المشاكل الشائعة

### المشكلة: "npm: command not found"
**الحل:** تأكد من تثبيت Node.js على جهازك

### المشكلة: "port already in use"
**الحل:** أغلق البرامج التي تستخدم نفس المنفذ أو غير المنفذ في ملفات الإعدادات

### المشكلة: "Cannot connect to database"
**الحل:** تأكد من اتصال الإنترنت لأن قاعدة البيانات على MongoDB Atlas (سحابية)

---

## 📝 ملخص الأوامر السريعة

```bash
# Terminal 1 - Backend
cd backend
npm run server

# Terminal 2 - Frontend  
cd frontend
npm run dev

# Terminal 3 - Admin
cd admin
npm run dev
```

---

## 🌐 العناوين بعد التشغيل

- **Backend API:** http://localhost:4000
- **Frontend:** http://localhost:5173
- **Admin Panel:** http://localhost:5174

---

**ملاحظة:** تأكد من أن جميع الأوامر تعمل بشكل صحيح قبل البدء في العمل على المشروع!






